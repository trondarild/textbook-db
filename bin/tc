#!/usr/bin/env bash
# tc — textbook-db claim matcher
# Usage: tc "theta oscillations coordinate memory during sleep"  →  txdb claim "..."

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_DIR="$(dirname "$SCRIPT_DIR")"

exec python3 "$REPO_DIR/txdb.py" claim "$@"
